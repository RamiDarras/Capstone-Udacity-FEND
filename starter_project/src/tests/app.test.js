import { handleGenerateTrip } from "../client/js/app";

describe("Test the handleGenerateTrip function", () => {
 test("It should be defined", () => {
  expect(handleGenerateTrip).toBeDefined();
 });
});
